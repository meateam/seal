export enum routeEnum{
  USER = '/user',
}

export enum environment{
  DEV = 'dev',
  PROD = 'prod',
  TESTING = 'testing',
}

export enum database{
  DEV = 'devDB',
  PROD = 'prodDB',
  TESTING = 'testingDB',
}
